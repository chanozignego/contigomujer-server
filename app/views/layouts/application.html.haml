!!!
%html
  %head
    %title Contigo Mujer
    = javascript_include_tag 'application'
    = csrf_meta_tags
    %meta{name: :viewport, content: "width=device-width, initial-scale=1"}
    = yield(:head)
  %body
    .page-wrapper.js-page-wrapper
      = render "layouts/flash_messages"
      = yield
      :javascript
        app.setLocale("#{I18n.locale}","#{I18n.default_locale}");
      = yield(:scripts)
